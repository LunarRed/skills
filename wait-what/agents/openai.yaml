interface:
  display_name: "Wait What"
  short_description: "Re-pitch that, simpler, with the context I'm missing"
  default_prompt: "Use $wait-what to re-pitch that."
policy:
  allow_implicit_invocation: false
